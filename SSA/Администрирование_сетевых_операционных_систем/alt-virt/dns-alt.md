---
title: Настройка dns-сервера
description: 
published: true
date: 2026-04-15T16:13:59.584Z
tags: 
editor: markdown
dateCreated: 2026-04-15T16:13:59.584Z
---

# Настройка DNS-сервера на ALT Linux в среде Proxmox VE
Цель работы:
Изучить принципы работы службы доменных имен DNS, освоить настройку DNS-сервера на базе **BIND9** в ОС **ALT Linux**, а также получить практические навыки создания **прямой** и **обратной** зон в виртуальной среде **Proxmox VE**.
## Теоретические сведения

**DNS (Domain Name System)** — это система доменных имен, предназначенная для преобразования символьных имен узлов (например, `server1.lab.local`) в IP-адреса (`192.168.100.10`) и обратно.

DNS-сервер позволяет:
- сопоставлять имена узлов IP-адресам;
- выполнять обратное преобразование IP-адресов в доменные имена;
- централизованно управлять именами компьютеров в сети;
- упрощать доступ к сетевым ресурсам.

В данной работе будет настроен:
- **первичный DNS-сервер**;
- **прямая зона** — для преобразования имени в IP;
- **обратная зона** — для преобразования IP в имя.

## Топология сети

В рамках работы используется одна внутренняя сеть в PVE.

### Сеть:
- **LAN:** `vmbr1`
- подсеть: `192.168.100.0/24`

### Виртуальные машины:
1. **VM1 — DNS-сервер**
   - ОС: ALT Linux
   - IP-адрес: `192.168.100.10`
   - Имя: `ns1.lab.local`

2. **VM2 — клиент**
   - ОС: ALT Linux
   - IP-адрес: `192.168.100.20`
   - Имя: `client1.lab.local`

> При необходимости можно добавить третью машину для дополнительной проверки, но для базовой лабораторной достаточно двух.

## Ход работы

## Шаг 1. Подготовка виртуальной среды в Proxmox VE

1. Откройте веб-интерфейс **Proxmox VE**.
2. Перейдите в раздел **Datacenter → Node → Network**.
3. Создайте новый Linux Bridge:
   - имя: `vmbr1`
   - **Bridge ports** оставить пустым

Это создаст **изолированную внутреннюю сеть**.

4. Создайте две виртуальные машины:
   - **DNS**
   - **Client**

5. Подключите сетевые адаптеры обеих ВМ к мосту `vmbr1`.

6. Установите на обе виртуальные машины **ALT Linux**.

## Шаг 2. Настройка сети на DNS-сервере

После установки ALT Linux необходимо настроить статический IP-адрес.

### 2.1. Определение имени сетевого интерфейса
Проверьте имя сетевого интерфейса:

```bash
ip a
```

Предположим, интерфейс называется `eth0` (на вашей виртуальной машине интерфейс будет называться иначе).

### 2.2. Настройка IP-адреса через etcnet

Создайте или отредактируйте файл:

```bash
vim /etc/net/ifaces/`eth0`/ipv4address #укажите ваш интерфейс
```

Содержимое:

```text
192.168.100.10/24
```

При необходимости задайте имя хоста:

```bash
hostnamectl set-hostname ns1.lab.local
```

Файл маршрута по умолчанию для этой лабораторной не обязателен, если интернет не нужен.  
Если нужен доступ в интернет через внешний шлюз, укажите его отдельно.

### 2.3. Настройка DNS-резолвера на самом сервере

На этапе первичной настройки можно временно указать внешний DNS:

```bash
vim /etc/net/ifaces/eth0/resolv.conf
```

```text
nameserver 8.8.8.8
```

Примените настройки:

```bash
systemctl restart network
```

Проверьте:

```bash
ip a
ping -c 3 127.0.0.1
```

## Шаг 3. Настройка сети на клиентской машине

На клиентской машине аналогично задайте статический адрес.

### 3.1. Настройка IP

```bash
vim /etc/net/ifaces/eth0/ipv4address
```

```text
192.168.100.20/24
```

### 3.2. Установка имени узла

```bash
hostnamectl set-hostname client1.lab.local
```

### 3.3. Указание DNS-сервера

На клиенте в качестве DNS укажем IP-адрес будущего DNS-сервера:

```bash
vim /etc/net/ifaces/eth0/resolv.conf
```

```text
nameserver 192.168.100.10
```

Примените настройки:

```bash
systemctl restart network
```

Проверьте связь с DNS-сервером:

```bash
ping -c 3 192.168.100.10
```

## Шаг 4. Установка DNS-сервера BIND на ALT Linux

На машине **VM1 (DNS-сервер)** установите BIND:

```bash
apt-get update
apt-get install bind bind-utils
```

> В некоторых сборках ALT Linux названия пакетов могут незначительно отличаться. Если пакет `bind-utils` отсутствует, можно установить доступные утилиты через поиск:
```bash
apt-cache search bind
```

После установки включите и запустите службу:

```bash
systemctl enable --now bind
```

Проверьте статус:

```bash
systemctl status bind
```

---

## Шаг 5. Настройка DNS-сервера

Основной конфигурационный файл BIND обычно находится здесь:

```bash
/etc/bind/options.conf
```

или используются дополнительные файлы в каталоге:

```bash
/etc/bind/
```

Перед редактированием желательно сделать резервную копию:

```bash
cp -r /etc/bind /etc/bind.bak
```

## Шаг 6. Создание прямой и обратной зон

### 6.1. Настройка файла локальных зон

Откройте файл конфигурации локальных зон. В ALT Linux он может быть организован через включаемые файлы. Удобно создать отдельный файл, например:

```bash
vim /etc/bind/local.conf
```

Добавьте в него описание двух зон:

```conf
zone "lab.local" {
    type master;
    file "/etc/bind/zones/db.lab.local";
};

zone "100.168.192.in-addr.arpa" {
    type master;
    file "/etc/bind/zones/db.192.168.100";
};
```

Теперь убедитесь, что этот файл подключается в общей конфигурации BIND.  
Если нет, добавьте строку include в основной конфиг, например в `/etc/bind/options.conf` или другой главный файл конфигурации:

```conf
include "/etc/bind/local.conf";
```

> Конкретный главный конфигурационный файл может зависеть от версии ALT Linux. Если нужно, можно проверить:
```bash
named-checkconf -p
```

## Шаг 7. Создание файлов зон

Создайте каталог для зон:

```bash
mkdir -p /etc/bind/zones
```

### 7.1. Файл прямой зоны

Создайте файл:

```bash
vim /etc/bind/zones/db.lab.local
```

Содержимое:

```dns
$TTL 86400
@   IN  SOA ns1.lab.local. admin.lab.local. (
        2026041501 ; Serial
        3600       ; Refresh
        1800       ; Retry
        604800     ; Expire
        86400 )    ; Minimum TTL

@       IN  NS      ns1.lab.local.
ns1     IN  A       192.168.100.10
client1 IN  A       192.168.100.20
www     IN  A       192.168.100.20
```

### Пояснение:
- `SOA` — основная запись зоны;
- `NS` — запись сервера имен;
- `A` — соответствие имени IP-адресу.

### 7.2. Файл обратной зоны

Создайте файл:

```bash
vim /etc/bind/zones/db.192.168.100
```

Содержимое:

```dns
$TTL 86400
@   IN  SOA ns1.lab.local. admin.lab.local. (
        2026041501 ; Serial
        3600
        1800
        604800
        86400 )

@       IN  NS      ns1.lab.local.
10      IN  PTR     ns1.lab.local.
20      IN  PTR     client1.lab.local.
```

### Пояснение:
- `PTR` используется для обратного разрешения адреса в имя.

## Шаг 8. Проверка синтаксиса конфигурации

Перед перезапуском службы необходимо проверить конфигурацию.

### 8.1. Проверка общего конфига

```bash
named-checkconf
```

Если ошибок нет, команда ничего не выводит.

### 8.2. Проверка прямой зоны

```bash
named-checkzone lab.local /etc/bind/zones/db.lab.local
```

### 8.3. Проверка обратной зоны

```bash
named-checkzone 100.168.192.in-addr.arpa /etc/bind/zones/db.192.168.100
```

Если всё правильно, вы увидите сообщение вида:

```text
OK
```

## Шаг 9. Перезапуск DNS-сервера

После проверки перезапустите службу:

```bash
systemctl restart bind
```

Проверьте статус:

```bash
systemctl status bind
```

Если служба не запускается, посмотрите журнал:

```bash
journalctl -xeu bind
```

## Шаг 10. Проверка работы DNS-сервера на самом сервере

На DNS-сервере выполните:

```bash
dig @127.0.0.1 ns1.lab.local
```

Проверка клиента:

```bash
dig @127.0.0.1 client1.lab.local
```

Проверка обратной зоны:

```bash
dig @127.0.0.1 -x 192.168.100.20
```

Также можно использовать `nslookup`:

```bash
nslookup client1.lab.local 127.0.0.1
nslookup 192.168.100.20 127.0.0.1
```

## Шаг 11. Проверка работы DNS с клиентской машины

На клиентской машине проверьте содержимое файла DNS:

```bash
cat /etc/net/ifaces/eth0/resolv.conf
```

Должно быть:

```text
nameserver 192.168.100.10
```

Теперь проверьте разрешение имени:

```bash
nslookup ns1.lab.local
```

```bash
nslookup client1.lab.local
```

```bash
nslookup www.lab.local
```

Проверка обратного разрешения:

```bash
nslookup 192.168.100.10
```

```bash
nslookup 192.168.100.20
```

Если всё настроено правильно, клиент должен получать ответы от DNS-сервера.

## Шаг 12. Настройка использования локального DNS на самом сервере

После успешной проверки можно указать DNS-серверу использовать самого себя.

На **VM1** отредактируйте:

```bash
vim /etc/net/ifaces/eth0/resolv.conf
```

Содержимое:

```text
nameserver 127.0.0.1
```

Примените:

```bash
systemctl restart network
```

Проверьте:

```bash
nslookup client1.lab.local
```

---

# Ожидаемый результат

В результате выполнения практической работы должен быть получен работающий DNS-сервер, который:

- разрешает имя `ns1.lab.local` в `192.168.100.10`;
- разрешает имя `client1.lab.local` в `192.168.100.20`;
- разрешает имя `www.lab.local` в `192.168.100.20`;
- выполняет обратное преобразование:
  - `192.168.100.10 → ns1.lab.local`
  - `192.168.100.20 → client1.lab.local`

---

# Типичные ошибки

## 1. Ошибка в имени зоны
Неправильно указано имя зоны в конфигурации или в команде проверки.

Пример правильной обратной зоны:

```text
100.168.192.in-addr.arpa
```

## 2. Отсутствует точка в конце FQDN
В DNS-записях полные доменные имена следует завершать точкой:

Правильно:
```dns
ns1.lab.local.
```

Неправильно:
```dns
ns1.lab.local
```

Без точки BIND может добавить имя зоны повторно.

## 3. Не увеличен Serial
После изменения файла зоны необходимо увеличивать номер `Serial` в записи `SOA`.

Например:
```dns
2026041501
```

При следующем изменении:
```dns
2026041502
```

## 4. Ошибка в PTR-записи
В обратной зоне указывается только последний октет IP-адреса.

Правильно:
```dns
20 IN PTR client1.lab.local.
```

## 5. Клиент не использует нужный DNS
Нужно проверить файл:

```bash
/etc/net/ifaces/eth0/resolv.conf
```

# Контрольные вопросы
1. Что такое DNS и для чего он используется?
2. В чем различие между прямой и обратной зонами DNS?
3. Для чего служат записи типов `A`, `NS`, `SOA` и `PTR`?
4. Почему в DNS-записях полное доменное имя часто записывается с точкой в конце?
5. Что такое FQDN?
6. Для чего нужен параметр `Serial` в записи `SOA`?
7. Почему после изменения зоны необходимо увеличивать `Serial`?
8. Как проверить конфигурацию BIND перед запуском службы?
9. Какая команда используется для проверки прямой зоны?
10. Какая команда используется для проверки обратной зоны?
11. Почему клиентская машина должна указывать IP-адрес DNS-сервера в `resolv.conf`?
12. Что произойдет, если обратная зона не настроена?